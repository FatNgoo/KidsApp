<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Crime Scene Background -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/bg_crime_scene"
        android:scaleType="centerCrop"
        android:contentDescription="Crime scene background" />

    <!-- Hidden Clue 1: Footprint (Top-left area) -->
    <ImageView
        android:id="@+id/clue_footprint"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="80dp"
        android:layout_marginTop="120dp"
        android:src="@drawable/clue_footprint"
        android:tag="footprint"
        android:contentDescription="Hidden footprint clue" />

    <!-- Hidden Clue 2: Magnifier (Center-right area) -->
    <ImageView
        android:id="@+id/clue_magnifier"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="250dp"
        android:layout_marginTop="300dp"
        android:src="@drawable/clue_magnifier"
        android:tag="magnifier"
        android:contentDescription="Hidden magnifier clue" />

    <!-- Hidden Clue 3: Key (Bottom-left area) -->
    <ImageView
        android:id="@+id/clue_key"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="120dp"
        android:layout_marginTop="550dp"
        android:src="@drawable/clue_key"
        android:tag="key"
        android:contentDescription="Hidden key clue" />

    <!-- HUD: Score Display -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:background="#AA000000"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/tv_score"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:text="Clues Found: 0/3"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold" />

    </LinearLayout>

</FrameLayout>
